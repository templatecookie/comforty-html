{
  "scripts": {
    "dev": "npx tailwindcss -i ./src/styles.css -o ./public/css/tailwind.css",
    "watch": "npm run dev --watch",
    "build": "npm run dev --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.2.4"
  }
}
